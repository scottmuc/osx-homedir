#!/usr/bin/env bash

# want to see the command that is being executed
set -x

directory=$(pwd)

if [ "${directory}" = "/" ]; then
  echo um, I do not think that is what you wanted to do
  exit 1
fi

find "${directory}" -name ".*" ! -name "." ! -name ".." -exec rm -rf {} \;

